# دليل إضافة Firebase إلى المشروع

## 📋 الخطوات المطلوبة:

### الخطوة 1: إنشاء مشروع Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اضغط على **"Add project"** أو **"إضافة مشروع"**
3. أدخل اسم المشروع (مثلاً: "Warm Clouds")
4. اختر **"Continue"** أو **"متابعة"**
5. (اختياري) فعّل Google Analytics إذا أردت
6. اضغط **"Create project"** أو **"إنشاء المشروع"**
7. انتظر حتى يتم إنشاء المشروع

### الخطوة 2: إضافة تطبيق Android

1. في صفحة المشروع، اضغط على أيقونة **Android** (أو "Add app")
2. أدخل **Package name**: `com.warmclouds.app`
   - ⚠️ **مهم جداً**: يجب أن يكون مطابق تماماً لـ `applicationId` في `build.gradle.kts`
3. أدخل **App nickname** (اختياري): "Warm Clouds"
4. (اختياري) أدخل **Debug signing certificate SHA-1** - يمكنك تخطيها الآن
5. اضغط **"Register app"** أو **"تسجيل التطبيق"**

### الخطوة 3: تحميل ملف google-services.json

1. بعد تسجيل التطبيق، سيظهر لك زر **"Download google-services.json"**
2. اضغط على الزر لتحميل الملف
3. **ضع الملف في المجلد**: `app/` (بجانب `build.gradle.kts`)
   - المسار الكامل: `app/google-services.json`

### الخطوة 4: تفعيل Firebase Services

#### أ. تفعيل Authentication (المصادقة):

1. في Firebase Console، اذهب إلى **Authentication**
2. اضغط على **"Get started"** أو **"ابدأ"**
3. اختر **Sign-in method** أو **طريقة تسجيل الدخول**
4. فعّل **Email/Password**:
   - اضغط على **Email/Password**
   - فعّل **Enable**
   - اضغط **Save**

#### ب. تفعيل Firestore Database:

1. في Firebase Console، اذهب إلى **Firestore Database**
2. اضغط على **"Create database"** أو **"إنشاء قاعدة بيانات"**
3. اختر **Start in test mode** (للاختبار) أو **Production mode** (للإنتاج)
4. اختر **Location** (الموقع) - اختر أقرب موقع لك
5. اضغط **"Enable"** أو **"تفعيل"**

#### ج. (اختياري) تفعيل Storage:

1. في Firebase Console، اذهب إلى **Storage**
2. اضغط على **"Get started"** أو **"ابدأ"**
3. اتبع التعليمات لتفعيل Storage

### الخطوة 5: تفعيل Firebase في المشروع

بعد إضافة ملف `google-services.json`، سأقوم بتفعيل الكود المعلق في المشروع.

---

## ✅ بعد إضافة الملف:

1. تأكد من وجود `google-services.json` في مجلد `app/`
2. سأقوم بتفعيل Firebase dependencies في `build.gradle.kts`
3. سأقوم بتفعيل Google Services plugin
4. سأقوم بتحديث Activities لاستخدام Firebase بدلاً من البيانات التجريبية

---

## 🔍 التحقق من الإعداد:

بعد إضافة الملف وتفعيل الكود:
1. اضغط **Sync Project with Gradle Files** في Android Studio
2. تأكد من عدم وجود أخطاء
3. شغّل التطبيق

---

## 📝 ملاحظات مهمة:

- ⚠️ **Package Name**: يجب أن يكون `com.warmclouds.app` في Firebase Console
- ⚠️ **ملف google-services.json**: يجب أن يكون في `app/` وليس في `app/src/main/`
- ⚠️ **Security Rules**: تأكد من إعداد قواعد الأمان في Firestore بعد الاختبار

---

## 🆘 إذا واجهت مشاكل:

1. تأكد من أن Package Name مطابق
2. تأكد من وجود ملف `google-services.json` في المكان الصحيح
3. قم بـ **Sync Project with Gradle Files**
4. قم بـ **Invalidate Caches / Restart**


