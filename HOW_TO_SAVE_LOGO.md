# كيفية حفظ شعار التطبيق (WARM CLOUDS)

## الخطوات:

### 1. حفظ الصورة في مجلد drawable:

**المكان:**
```
app/src/main/res/drawable/
```

**الخطوات:**
1. افتح مجلد المشروع: `MyApplication3`
2. اذهب إلى: `app/src/main/res/drawable/`
3. انسخ ملف الصورة هنا
4. أعد تسمية الملف إلى: `logo_warm_clouds.png` (أو `.jpg`)

**ملاحظات مهمة:**
- ✅ اسم الملف يجب أن يحتوي على أحرف صغيرة فقط
- ✅ يمكن استخدام أرقام و underscore (_)
- ✅ لا تستخدم مسافات أو أحرف خاصة
- ✅ الصيغ المدعومة: `.png`, `.jpg`, `.webp`

**أمثلة لأسماء صحيحة:**
- ✅ `logo_warm_clouds.png`
- ✅ `app_logo.png`
- ✅ `warm_clouds_logo.jpg`
- ❌ `Logo Warm Clouds.png` (خطأ - يحتوي على مسافات وأحرف كبيرة)

---

### 2. استخدام الصورة في التطبيق:

بعد حفظ الصورة، يمكن استخدامها في:

#### أ) في صفحة الترحيب (Welcome):
```xml
<ImageView
    android:layout_width="200dp"
    android:layout_height="200dp"
    android:src="@drawable/logo_warm_clouds"
    android:scaleType="centerCrop" />
```

#### ب) في الكود Java:
```java
ImageView logoImageView = findViewById(R.id.logoImageView);
logoImageView.setImageResource(R.drawable.logo_warm_clouds);
```

---

### 3. أحجام مختلفة (اختياري):

إذا أردت صور بأحجام مختلفة للشاشات المختلفة:

```
app/src/main/res/
  ├── drawable-mdpi/
  │   └── logo_warm_clouds.png (حجم أصغر)
  ├── drawable-hdpi/
  │   └── logo_warm_clouds.png
  ├── drawable-xhdpi/
  │   └── logo_warm_clouds.png
  ├── drawable-xxhdpi/
  │   └── logo_warm_clouds.png (حجم أكبر)
  └── drawable-xxxhdpi/
      └── logo_warm_clouds.png
```

**لكن:** يمكنك استخدام صورة واحدة في `drawable/` وسيقوم Android بتكبيرها تلقائياً.

---

## خطوات سريعة:

1. ✅ انسخ ملف الصورة
2. ✅ اذهب إلى: `app/src/main/res/drawable/`
3. ✅ الصق الملف هنا
4. ✅ أعد تسميته إلى: `logo_warm_clouds.png`
5. ✅ انتهى! 🎉

---

## نصيحة:

- استخدم صيغة PNG للشعارات (دعم الشفافية أفضل)
- حجم الصورة المثالي: 512x512 بكسل أو أكبر
- تأكد من أن الصورة واضحة وذات جودة عالية

